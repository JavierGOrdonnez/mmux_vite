# SPEC — MMUX backend (flaskapi/)

Caveman-encoded. Distilled from code 2026-05-28. Child of root spec.

## LINKS
- parent → [`../SPEC.md`](../SPEC.md) — orchestration, services, version
- sibling → [`../node/SPEC.md`](../node/SPEC.md) — frontend that consumes this `/flask/*` API

## §G
Flask API: relay frontend ↔ oSPARC (functions, jobs, collections, studies), generate samples (LHS / grid / single), run Dakota meta-modeling (SUMO surrogate, UQ propagation, MOGA optimization), persist state text files. Serve under `/flask/*`, port 5000.

## §C
- Python 3.11, `flask==3.1.1`, `flask-cors==6.0.0`, `gevent==25.5.1`
- run: dev `uv run python -m flask run` (entrypoint.sh), prod `uvx gunicorn main:app` (`main:app = create_flask_app()`)
- oSPARC client `osparc==0.8.3.post0.dev30`; Dakota `itis-dakota==1.5.9`
- numerics: `numpy==2.2.6`, `pandas==2.2.3`, `scipy==1.15.3`, `scikit-learn==1.6.1`
- `mmux_flaskapi.dakota` subpackage (inlined, own module namespace) → Dakota conf generation + result evaluation + `lhs()` — was vendored `mmux_python` dep, ported in-repo (§T15)
- requests accept camelCase|snake_case (pydantic `populate_by_name`); responses camelCase
- `DataPreprocessor` maps orig var names → `x1..xn`,`y1..yn` for Dakota, inverse on response
- ≥5 completed jobs required for any surrogate/UQ/MOGA endpoint
- ruff line-length 100, select E/F/I/UP; pytest markers slow/integration/unit; coverage aim ≥70% on modified code (soft)
- naming: Classes PascalCase | funcs/methods snake_case | constants CONSTANT_CASE | private `_`prefix
- type hints + PEP257/NumPy docstrings on public APIs; raise ValueError/RuntimeError w/ descriptive msg; log via `utils/logger.py`
- type checker: `ty` (astral-sh/ty), config via `[tool.ty]` in pyproject.toml; strictness/rule-level TBD in §T18

## §I
parent: [`../SPEC.md`](../SPEC.md) ; frontend consumer: [`../node/SPEC.md`](../node/SPEC.md) §I
factory: `create_flask_app() -> MMUXFlask` registers 5 blueprints under `/flask`
--- deployment_bp `/flask/deployment` ---
api: GET `/health` → 200 `{status:"healthy"}` (docker healthcheck)
api: GET `/service-mode` → `{service_mode:<env SERVICE_MODE>}`
api: GET `/permissions` → `{permissions:<env PERMISSIONS>}`
api: GET `/mode` → `{deployment_mode:<env DEPLOYMENT_MODE>}`
--- osparc_bp `/flask/osparc` ---
api: GET `/list_functions` → Function[]
api: GET `/list_jobs` → FunctionJob[]
api: GET `/list_function_job_collections` → Collection[]
api: GET `/list_function_jobs_for_functionid?functionUid=` → FunctionJob[] (+status each)
api: GET `/list_function_jobs_for_jobcollectionid?JobCollectionUid=` → FunctionJob[]
api: GET `/list_function_job_collections_for_functionid?functionUid=` → Collection[]
api: GET `/get_function_job?jobUid=` → `{uid,status,outputs}`
api: GET `/get_function_job_status?jobUid=` → `{status}`
api: GET `/get_function_job_outputs?jobUid=` → outputs
api: GET `/download_job_collection_csv?JobCollectionUid=` → text/csv (metadata preamble lines + jobs table; preamble = `# key,value`, table = inputs+outputs cols) — distilled from feature/local-functions, port (§T6)
--- textfile_bp `/flask/text-file` ---
api: POST `/` `{filename,content}` → `{status:"success",filename}`
api: GET `/<filename>` → `{filename,content}` | 404
--- sampling_bp `/flask/sampling` ---
api: POST `/lhs` `{funUid,config[{variable,start,end}],seed,n}` → job collection (camelCase)
api: POST `/grid` `{funUid,config[{variable,start,end,steps}]}` → job collection
api: POST `/test_job` `{funUid,config[{variable,value}]}` → job {status,inputs,outputs}
api: POST `/clone_job` `{functionName,projectJobId,projectInputs}` → study
api: POST `/upload_job_collection_csv` `{csvContent}` → parse preamble+table → reconstruct job collection; ? create local function from source if uid ∉ oSPARC (local_job_store) — distilled from feature/local-functions, port (§T6)
--- dakota_bp `/flask/dakota` ---
api: POST `/sumo_cross_validation` `{inputVars[],output,FunctionJobs[]}` → `{outputName,outputNameHat,outputNameStdHat}`
api: POST `/manual_uq_propagation_with_uncertainty` `{output,inputVars[],distributions,numSamples,FunctionJobs[],nHistograms,seed}` → histogram+box stats
api: POST `/sumo_along_axes` `{output,inputs[],FunctionJobs[],sliderValues?}` → `{predictions:{var:{x,yHat,stdHat}}}`
api: POST `/sumo_grid_evaluation` `{output,gridVars[],inputVars[],FunctionJobs[],sliderValues?}` → `{gridData}`
api: POST `/get_sumo_cv_accuracy_metrics` `{inputs[],output,FunctionJobs[]}` → `{metrics,tTest?:{statistic,pValue},convergence:[{nSamples,metric}]}` — computed from `evaluate_sumo_manual_crossvalidation`'s actual/predicted arrays (⊥ Dakota stdout log parsing, see B11)
api: POST `/perform_moga_optimization` `{inputVars[],distributions,outputVarSelection{var:minimize|maximize},FunctionJobs[]}` → `{optimizationResults}`
api: POST `/compute_correlation_indices` `{output,inputVars[],distributions,numSamples,FunctionJobs[],seed}` → `{correlations:{inputVar:{pearson,spearman}}}` (#470)
api: POST `/compute_sobol_indices` `{output,inputVars[],distributions,numSamples,FunctionJobs[],seed(≥0)}` → `{sobol:{inputVar:{main,total,mainCiLow,mainCiHigh,totalCiLow,totalCiHigh}}, sobolSecondOrder:{varA:{varB:float}}}` — scipy-based Saltelli sampling + `scipy.stats.sobol_indices`, always-on second-order + bootstrap CIs on first/total-order (#470, §T20,T23,T25); `numSamples` in request ⊥ used for Sobol' base count (V36)
--- env ---
env: `OSPARC_API_BASE_URL`,`OSPARC_API_KEY`,`OSPARC_API_SECRET` ! set
env: `SERVICE_MODE`,`PERMISSIONS`,`DEPLOYMENT_MODE` (surfaced by deployment_bp)
env: `OSPARC_NODE_ID`,`OSPARC_STUDY_ID` ? (req when DEPLOYMENT_MODE=OSPARC, else "null")
env: `LOG_LEVEL` ? default `DEBUG`
--- lib mmux_flaskapi.dakota public surface (inlined subpackage, `src/mmux_flaskapi/dakota/`) ---
lib: `lhs(n,k,seed)` → normalized [0,1] sample matrix
lib: `create_grid_samples()`,`create_manual_uq_samples()`,`create_samples_along_axes()`
lib: `DakotaObject.run(conf,output_dir)` → subprocess `dakota.environment.study()`
lib: `create_{sumo_evaluation|sumo_crossvalidation|sumo_manual_crossvalidation|moga_optimization|uq_propagation}_conffile()`
lib: `evaluate_sumo()`,`evaluate_sumo_along_axes()`,`evaluate_sumo_on_grid()`,`evaluate_sumo_crossvalidation()`,`evaluate_sumo_manual_crossvalidation()`,`perform_moga_optimization()`,`propagate_uq()`,`evaluate_sobol_indices()` (scipy-based Saltelli sampling + first/total/second-order)
--- util surface (distilled from feature/local-functions, to port) ---
util: `utils/local_job_store.py` → JSON-backed store for synthetic local functions/collections/jobs (no live oSPARC). uid-prefix detect `is_local_function_uid`/`is_local_job_collection_uid`/`is_local_job_uid`; CRUD `create_local_function`/`create_local_job_collection`/`list_local_*`/`get_local_*`/`list_local_jobs_for_collection` (§T7)
util: `utils/case_preserving.py` → `PreserveCaseTransform`, `FunctionVariablesDict`/`FunctionVariableStr` wrappers, `has_preserve_case_metadata(metadata)` → keep orig variable-name case through serialization (§T8)
util: `json_serializer.recursive_dict_keys_snake_to_camel(d, preserve_nested_keys={"inputs","outputs","default_inputs","properties"})` → value-key dicts ∉ snake↔camel convert (§T8)

## §V
V1: `create_flask_app()` registers exactly 5 bp {deployment,osparc,text-file,sampling,dakota} under `/flask/*`
V2: ∀ dakota endpoint → ≥`max(5, len(input_vars)+1)` completed jobs else 400 (dimension-scaled minimum, V30, closes B30); job complete ⟺ `status.lower() ∈ {"completed","success"}`
V3: requests parse camelCase|snake_case (pydantic `populate_by_name=True`); JSON responses camelCase (e.g. `drag_force` → `dragForce`)
V4: `DataPreprocessor` maps orig→`x1..`,`y1..` before Dakota, `inverse_transform` back on response; mapping persisted `preprocessor_config.json`
V5: UQ-with-uncertainty needs `{output}_std_hat` in the surrogate's `evaluate_sumo()` results (variance is a property of the fitted model, computed from `variances.dat` when Dakota produces it — never a pre-existing key on raw `function_jobs[].outputs`, see V32/B12); noise uses `scipy.special.erfinv` as `r = sqrt(2)*erfinv(uniform(-1,1))` (~N(0,1); Phi(x) = (1+erf(x/sqrt(2)))/2 — sqrt(2) factor required, bare erfinv has std 1/sqrt(2)≈0.707 and understates uncertainty ~29%, closes B16/B29)
V6: MOGA `maximize` objective → sign-switch to internal minimize, inverse on result
V7: `DEPLOYMENT_MODE=LOCAL` → parent node/project ids = `"null"`; `=OSPARC` → read `OSPARC_NODE_ID`/`OSPARC_STUDY_ID`; other → ValueError
V8: text-file `filename` rejects path separators (⊥ traversal); root `/text-files/`
V9: GET `/health` → 200 `{status:"healthy"}` (matches docker HEALTHCHECK & Caddy `health_uri`)
V10: `OSPARC_API_{BASE_URL,KEY,SECRET}` ! set → `OsparcApi` init (BASE_URL `.rstrip("/")`)
V11: error map (`@api_endpoint`): KeyError→400, ValueError→422, OsparcApiException→its status, else→500
V12: sampling executes via oSPARC `functions_api.map_function(...)` (lhs/grid) / `run_function(...)` (test_job), inputs validated by `validate_function_inputs`
V13: request parser + response serializer ⊥ snake↔camel-convert keys ∈ value dicts `{inputs,outputs,default_inputs,properties,slider_values,distributions,output_var_selection,project_inputs}` (these are data/variable names, not API fields); both directions share `helpers._DEFAULT_PRESERVE_NESTED_KEYS`/`preserve_nested_keys` param (§T8 done)
V14: orig variable-name case preserved through `DataPreprocessor` x1..xn round-trip + serialization (⊥ lowercase); preserve-case driven by function metadata (`has_preserve_case_metadata`) — distilled, to port (§T16, remaining half of former §T8)
V15: functions/collections/jobs MAY resolve from `local_job_store` w/o live oSPARC, ∀ `DEPLOYMENT_MODE` (⊥ gated to LOCAL — reversed by T22/B21: local-store availability and remote-backend availability are orthogonal, oSPARC can be fully live in prod while local/CSV-imported functions stay usable); uid-prefix routes local-vs-oSPARC ∀ osparc/sampling/dakota lookup — distilled, ported (§T7)
V16: log-scale per-variable flag (from request payload) reaches Dakota preprocessing → sample/train in log space, inverse on response (⊥ train linear when UI=log) — distilled, to port (§T9, node/SPEC.md V12)
--- review-backprop invariants (Copilot review on #467; bugs §B1-B5, fixes §T10-T14) ---
V17: `local_job_store` dir anchored to explicit base (env `LOCAL_STORE_DIR` or `Path(__file__).resolve().parents[N]`), ⊥ `Path.cwd()`-derived; mkdir deferred to first write + `parents=True` (B1)
V18: response ⊥ emit same datum under both snake+camel key when global serializer camel-converts (⊥ pre-add `jobIds` beside `job_ids`) → ⊥ key-collision overwrite (B2)
V19: CSV cell parse ! raise ValueError w/ row+col ctx on unparseable non-blank cell; truly-blank → NaN sentinel, ⊥ silent `0.0` (⊥ feed accidental zeros to Dakota) (B4)
V20: `local_job_store._load_store` catches only `(OSError, json.JSONDecodeError)`; on corrupt JSON ! backup offending file, ⊥ silent reset-then-overwrite (⊥ unrecoverable loss) (B5)
V21: `_get_all_items` ! loop forever on empty page; empty `response.items` → break after current page, return accumulated items
V22: recursive dict key converters ! mutate input dict/list in-place; conversion returns new object, caller input preserved
V23: `sampling.test_job` polling exit depends on `job["status"]` string, not dict keys; `FAILURE` in status → break
V24: `_anonymize(s, n, m=None)` on non-empty `s` ! expose full string; omitted `m` always masks at least one char
V25: Dakota endpoints ! call `os.chdir()`; run dirs use explicit paths only, request cwd stays process-global and unchanged
V26: SuMo cross-validation accuracy response includes a paired t-test (statistic+p-value) on CV actual-vs-predicted residuals, surfacing systematic surrogate bias beyond scalar MAE/RMSE
V27: SuMo CV accuracy metrics available as a convergence series `{n_samples:metric}` across increasing training-sample-count subsets, ⊥ single-N snapshot only
V28: `POST /flask/dakota/compute_correlation_indices` generates a Monte Carlo sample set from per-input distributions (same `create_manual_uq_samples()` used by UQ propagation), evaluates it via `evaluate_sumo()`, then `compute_correlation_indices()` (`dakota/funs_data_processing.py`) computes per-input↔output Pearson+Spearman coefficients (`scipy.stats.pearsonr`/`spearmanr`) between each input variable's samples and the predicted QoI values; response `{correlations:{inputVar:{pearson,spearman}}}` covers ∀ requested input vars in one call (⊥ 3-var limit of 1D/2D/3D plot views) (#470)
V29: `recursive_dict_keys_{camel_to_snake,snake_to_camel}` ⊥ convert keys nested *inside* a `preserve_nested_keys` value-dict (default `_DEFAULT_PRESERVE_NESTED_KEYS`); param overridable, not hardcoded; applies uniformly to request parsing, response serialization, AND `_get_all_items`/`_get_first_N_items`/`_get_last_N_items` SDK ingestion since all route through these two functions (closes B11); FE `opaqueValueDictKeys` (`functionUtils.ts`, read-path only) ⊆ this set, asserted by cross-language test `test_preserve_nested_keys_matches_frontend_opaque_keys` — no shared runtime file (../node/SPEC.md V19)
V30: Dakota surfpack GP surrogate build aborts (MODEL_ERROR/exit-250) when training points ≤ input dimensionality; `required_completed_jobs(input_vars, floor=5) = max(floor, len(input_vars)+1)` gates all 6 job-consuming request models + `JobVariableSelection`, else clean 400 (closes B30)
V31: strict remote oSPARC endpoints ! call the configured SDK surface and propagate downstream `OsparcApiException` status/body; connectivity probing is confined to optional local-fallback helpers (`get_osparc_api_if_connected`), ⊥ preflight network test in `get_osparc_api` that masks endpoint-specific SDK errors (B14)
V32: `POST /flask/dakota/compute_sobol_indices` builds a surrogate from completed jobs via `evaluate_sumo()`, then computes Sobol' indices directly in Python: generate Saltelli A/B/AB sample matrices locally (honouring per-input distributions via `scipy.stats.rv_continuous.ppf`; `constant`-distribution vars excluded from sampling, returned as `{main:0.0,total:0.0}`), evaluate all samples in ONE batch through `evaluate_sumo()` (surrogate-only, Dakota now ⊥ run `variance_based_decomp` itself), then apply `scipy.stats.sobol_indices` for first-order + total-order indices (single-varying-variable case bypasses scipy, computed via direct Saltelli 2010 covariance formulas since scipy's array-squeeze crashes at d=1) + a closed-form second-order (pairwise interaction) estimator (Jansen/Saltelli 2010; reuses f_A/f_B/f_AB_i/f_AB_j arrays, no extra surrogate evaluations); response `{sobol:{inputVar:{main,total}}, sobolSecondOrder:{varA:{varB:float}}}` covers ∀ 3 orders simultaneously (⊥ opt-in; free since same batch); `numSamples` rounded up to next power-of-2 before sampling (Sobol' QMC requirement); response indices ⊥ validated non-negative/bounded (small-N MC noise yields small negative estimates, only `isfinite` enforced) (#470, #485)
V33: `_dak_exec_static` ! read back captured `./stdout`/`./stderr` on both the success AND failure path of `study.execute()` AND `dakenv.study(...)` construction (NIDR input-file parsing happens at construction, before `.execute()`); on failure, wrap Dakota's own exception in a `RuntimeError` whose message embeds the captured stdout+stderr, so callers/API error responses surface Dakota's real diagnostic text (e.g. the NIDR/input-file line that triggered an abort) instead of only Dakota's generic top-level exception ("Dakota aborted: Unknown error 254") (closes B16, B17)
V34: `SobolIndicesRequest.seed` accepts `0` (`ge=0`, numpy/scipy RNGs accept 0, unlike Dakota's NIDR parser which required `seed ≥ 1`, B17) — the NIDR constraint no longer applies now that V32 has migrated off Dakota's `variance_based_decomp`
V35: Sobol' second-order (pairwise interaction) indices are ALWAYS computed alongside first/total (V32 scipy-based approach, same batch); `SobolIndicesRequest` has no `secondOrder` opt-in flag (never added); response always includes `sobolSecondOrder:{varA:{varB:float}}` symmetric ∀ unordered pairs, no self-pair (diagonal filled on frontend from first-order index); formula: closed-form Jansen/Saltelli estimator reusing the same f_A/f_B/f_AB sample evaluations (Saltelli et al. 2010), validated vs Ishigami analytical reference (R1) via `test_sobol_indices_ishigami_analytical`
V36: Sobol' base sample count is a fixed module constant `SOBOL_BASE_SAMPLES=1024` (`dakota/funs_evaluate.py`), NOT the request's `numSamples` field (that field stays on `SobolIndicesRequest` only for schema compat w/ `ManualUQPropagationRequest`, shared by Histogram/Correlation, and is ignored by `evaluate_sobol_indices`); rationale: Sobol' cost is multiplicative `SOBOL_BASE_SAMPLES*(d_varying+2)` (Saltelli scheme) unlike the other UQ views' additive cost, so reusing the shared UQ default (10,000→16,384 rounded) would multiply out to 5-10x more surrogate evaluations than needed; N=1024 matches common SALib/scipy tutorial defaults for reliable index estimates at typical dimensionalities
V37: first-order/total-order Sobol' indices ship w/ bootstrap confidence intervals (`main_ci_low`,`main_ci_high`,`total_ci_low`,`total_ci_high` per input var, 95% CI, `SOBOL_BOOTSTRAP_RESAMPLES=1000` resamples) computed by resampling the ALREADY-computed f_A/f_B/f_AB evaluations (row-index resampling w/ replacement) — no extra `evaluate_sumo()` calls, effectively free; d_varying≥2 uses scipy's native `SobolResult.bootstrap()`; d_varying==1 uses a hand-rolled percentile bootstrap (same closed-form S1/ST formulas as the point estimate) since scipy's `sobol_indices()` cannot be called directly at d=1 (pre-existing squeeze bug, V32); second-order heatmap explicitly EXCLUDED from CI display (user decision, out of scope for now)
V38: `load_data()`'s `.dat`/`.txt` branch (`funs_data_processing.py`) validates EVERY data row's whitespace-token count against the header's BEFORE constructing the DataFrame; a mismatch raises `ValueError` naming the file, header column count, offending line number, and that line's column count/content — ⊥ let pandas' generic `"X columns passed, passed data had Y columns"` (no file/line context) bubble up uncaught (closes B20); `evaluate_sumo_manual_crossvalidation` additionally wraps its per-fold `get_results(predictions.dat)` call, prefixing any parse failure with `"Fold {N} of {N_CROSS_VALIDATION}: ..."` so a malformed-file error names which CV fold produced it
V39: `load_data()`'s column-mismatch `ValueError` (V38) embeds the RAW (untokenized) previous/offending/next lines + total line count of the source file, not just the tokenized row — the offending run dir (e.g. a Dakota fold's `predictions.dat`) is not guaranteed to be reachable/still-present from wherever the error is diagnosed (different host/container than the dev machine, no run-dir cleanup exists but no guaranteed shared filesystem access either), so the raw bytes must travel inside the exception/log text itself to confirm or refute a formatting-cause hypothesis (e.g. Dakota tabular-writer line-wrapping) the next time this occurs (closes B21)
V40: `load_data(files, on_malformed_row="heal_or_drop", warnings=[...])` opt-in mode (default stays `"raise"`, ⊥ change for the ~10 unmodified call sites) recovers Dakota's confirmed duplicated-prefix tabular-writer defect (B22): `_try_heal_duplicated_prefix_row` reconstructs the row's dropped `_eval_id` as its own sequential line position (always safe, eval_id is sequential-by-construction) and keeps the row's LAST (fully-completed) write attempt for every other column; a row that doesn't match this exact corruption shape is DROPPED (⊥ raised) with a descriptive message appended to the caller's `warnings` list. `evaluate_sumo_manual_crossvalidation` uses this mode for both `predictions.dat`/`variances.dat`, maps each surviving row back to its original sample index via the row's OWN `_eval_id` value (`val_idx[eval_id-1]`, ⊥ by array position) so a dropped/healed row never misaligns any other fold's predictions, and pre-fills `all_predictions`/`all_stds` with `NaN` (`np.full`, ⊥ `np.empty`) so an unrecovered sample is explicit, not garbage; accumulated warnings surface in the result dict (`"warnings":[...]`, only if non-empty), propagated by `_inverse_transform_output_results` (`dakota.py`) into the `/sumo_cross_validation` JSON response. `compute_cv_accuracy_metrics`/`compute_paired_ttest` filter `NaN` actual/predicted pairs (`~np.isnan(...)` mask) before computing statistics, so one dropped CV point can't turn every metric into `NaN`; final response arrays replace any remaining `NaN` w/ JSON-safe `null` (`_replace_nan_with_none`) since Python's `json` module emits a literal `NaN` token that breaks `JSON.parse` on the frontend
V41: `evaluate_sumo_manual_crossvalidation` (`funs_evaluate.py`) MUST assert that `output_response` (mapped `y1`) is present in `predictions_df.columns` (and `output_response + "_variance"` in `variances_df.columns`) BEFORE column-access; an absent column SKIPS that fold (its validation points stay NaN), appends a fold+file-scoped `warnings` entry naming the run dir + available columns, and CONTINUES — ⊥ a bare `KeyError('y1')` 500 (closes B23). Resolution consistent with B22's user-chosen "heal or warn & continue": missing output column → ALWAYS 200 with `warnings` + NaN-filled `all_predictions`/`all_stds` (the `~np.isnan` metric filtering makes a fully-skipped output safe, not an error); there is NO hard 4xx, so a real-data QoI whose surrogate Dakota fails to emit still returns a deterministic (degraded) response. Test coverage MUST include (a) a forced missing-column case (monkeypatch `DakotaObject.run` / the conf to write a column-less `predictions.dat`) asserting 200 + a `warnings` entry, ⊥ `{"error":"'y1'"}`; and (b) a production-shaped (multi-input, real physics-named) request posted through the REAL Dakota path (the `material_sweep_first50_Osparc.csv` 50-job set, `output=delta_T_nerve`) asserting 200 + `warnings` — the well-conditioned synthetic fixture can't keep masking this gap (closes the B3-style blind spot behind B23)
V42: `/sumo_cross_validation` response nested under `cv_results` key (`jsonify({"cv_results": results_transformed})`); `cv_results` added to `helpers._DEFAULT_PRESERVE_NESTED_KEYS` so the global `after_request` camelCase serializer preserves variable-name keys (e.g. `delta_T_nerve`, `delta_T_nerve_hat`) inside the subtree — closes B24's multi-word QoI undefined-read bug. Frontend reads `data.cvResults[selectedQoI]` / `data.cvResults[`${selectedQoI}_hat`]` (SuMoValidation, SuMoStats). Regression tests verify both simple (`y`) and multi-word (`drag_force`) variable names survive the full round-trip with original names + suffixes intact
V43: `evaluate_sumo_manual_crossvalidation` (`funs_evaluate.py`) MUST treat a Dakota fold run that ABORTS (never writes `predictions.dat`) as a skip+continue, NOT a 500 — closes the residual B23 failure mode the real-data e2e exposed. Concretely: wrap `DakotaObject().run(...)` in try/except; if it raises OR `predictions.dat` is absent afterward, append a fold-scoped `warnings` entry and `continue` (its validation points stay `NaN`), ⊥ the previous `load_data(...)` raising an opaque `RuntimeError("failed to parse Dakota's predictions.dat (...)")` 500. Same "warn & continue" resolution as B22/B23: a fold whose surrogate training Dakota aborts returns 200 with NaN gaps + `warnings`, not a 500 that blanks the whole cross-validation (and, via SuMoValidation, the Inspect-Model plot). This guards the non-deterministic Dakota abort observed on the `material_sweep_first50_Osparc.csv` 50-job set (`output=delta_T_nerve`) — which fold aborts varies run-to-run, so without V43 the endpoint 500s intermittently and the e2e is non-deterministic. Both `/sumo_cross_validation` and `/get_sumo_cv_accuracy_metrics` call this function, so V43 covers both. Test coverage: a forced-abort case (monkeypatch `DakotaObject.run` to not write `predictions.dat`, or raise) asserting 200 + a `warnings` entry, ⊥ `{"error":...}` 500

V44: `create_manual_uq_samples` ! draw ∀ distribution sample via the seeded `np.random.Generator` (passed as scipy `random_state=`), never scipy's un-seeded global state — same seed + same request ⇒ byte-identical samples (closes B25)
V45: `get_osparc_api_if_connected()` logs the unreachable-backend WARNING once per unreachability episode (⊥ once per request/call while still down), re-arms (logs again) after a recovery + subsequent drop (closes B27)
V46: ∀ osparc.py endpoint keyed by a real (non-local) function/job-collection/job uid → unreachable-or-unconfigured oSPARC degrades to empty/local-only result (list endpoints) or a 422 ValueError (get-by-id endpoints incl. `_function_schema_vars`), ⊥ unconditional `get_osparc_api()` call crashing as a 500 — ∀ `DEPLOYMENT_MODE`, not just LOCAL (closes B31)
V47: `_parse_uploaded_job_collection_csv` ⊥ accept a CSV data row whose cell count ≠ header cell count (⊥ silent `dict(zip(header,cells))` truncation/misalignment); mismatched row → `ValueError` w/ row context → 422 (closes B33)
V48: `local_job_store._save_store` ⊥ write directly to `LOCAL_STORE_FILE`; writes to a sibling temp file first, then atomically `os.replace()`s over the target (temp file cleaned up on failure) so an interrupted/concurrent write never leaves a partially-written store file (closes B33)
--- review-backprop invariants (dakota/ cleanup pass; bugs §B34-B36, fix/tests §T32) ---
V49: `lhs()` method dispatch uses exact-match comparison (⊥ substring/`in` membership on a bare string) ∀ branch, incl. `lhsmu` (B34)
V50: `evaluate_sumo_crossvalidation` log_output fed to `_parse_crossvalidation_outputlogs` reads the actual captured Dakota stdout (`run_dir/dakota_stdout.txt`, written by `DakotaObject.run`), ⊥ hardcoded empty string (B35)
V51: `sanitize_varnames` char-class preserves literal `-` (placed last in `[...]`, ⊥ mid-class forming an unintended `*-+` range); e.g. default `get_results` key `-AFpeak` survives sanitization (B36)
V52: `ty check src/mmux_flaskapi` passes with zero errors; enforced blocking in pre-commit (local hook, mirrors eslint-node) and CI (prek job)
V53: osparc.py's local-merge (V15) + graceful-degradation (V46) behavior for functions/job-collections/jobs each have exactly ONE implementation point — `_get_osparc_api_if_available()` (availability gate) + `_ResourceKind` (bundles local_job_store/SDK accessors per kind) + the 3 generics `_list_merged`/`_get_by_id`/`_list_merged_for_parent` in osparc.py — endpoint handlers are thin wrappers calling these, ⊥ per-endpoint hand-rolled merge/degrade logic (closes T34's original duplication complaint; a future re-introduction of a `DEPLOYMENT_MODE` branch inside an endpoint handler, instead of inside these shared helpers, violates this)
V54: when EVERY fold of `evaluate_sumo_manual_crossvalidation`'s 5-fold CV fails (B23's per-fold "warn & continue"), leaving zero valid actual/predicted pairs, `/get_sumo_cv_accuracy_metrics` still returns 200 w/ NaN-valued `CVAccuracyMetrics` (⊥ `np.max` on an empty array) and a NaN-valued paired t-test (⊥ `compute_paired_ttest`'s pre-existing "≥2 samples" `ValueError`, caught at the route level ⊥ inside the stats helper itself, so `compute_paired_ttest`'s own raise-on-<2-samples contract, relied on directly by `test_funs_evaluate_cv_stats.py::test_too_few_samples_raises`, is unchanged)
V55: `JobVariableSelection` training records ! require every completed job to contain every selected variable; mixed-schema completed-job collections → hard 400 whose message states each missing selected input/output variable, its missing-row count, total completed-job count, all-fields-required rule, and aggregate available keys; ⊥ generic "not found" wording that contradicts available-key diagnostics (B39)

## §R
R1: Ishigami test function (3-param) published analytical Sobol' indices (Saltelli 2007/2008), validating the scipy migration (V32/V35): first-order S1≈0.314, S2≈0.442, S3≈0; total-order S1_total≈0.558, S2_total≈0.442, S3_total≈0.244; second-order S_12≈0, S_13≈0.244, S_23≈0. Backed by `test_sobol_indices_ishigami_analytical` (flaskapi/tests/test_sobol_indices.py)

## §T
id|status|task|cites
T1|.|frontend expects `/flask/osparc/download_job_collection_csv` & `/flask/sampling/upload_job_collection_csv` — IMPLEMENTED on feature/local-functions; resolved-by → port via §T6|T6, ../node/SPEC.md T1
T2|x|`pyproject.toml` & `mmux_python/pyproject.toml` version `1.5.14` ≠ service `1.5.18`; add to `.bumpversion.cfg` or align — superseded by T15 (mmux_python removed, no more separate versioned pkg to drift)|../SPEC.md V5,T1,T15
T3|x|`/get_sumo_cv_accuracy_metrics` now consumed by frontend `SuMoValidation` view (bias banner + convergence curve, T18/../node/SPEC.md T20) — resolved|I,T18,../node/SPEC.md T20
T4|.|`tests/implementation instructions/` + `tests/logs/` in tests tree — relocate to `docs/` or gitignore|—
T5|.|add explicit test asserting all 5 blueprints + every route registered (guards V1)|V1
T6|.|PORT [topic=fullstack-csv] job-collection CSV import/export: GET `/osparc/download_job_collection_csv` (preamble+table) + POST `/sampling/upload_job_collection_csv` (parse→reconstruct). reuse branch helpers `_split_csv_preamble_and_table`/`_parse_uploaded_job_collection_csv`/`_job_collection_jobs_to_csv`; add tests|I, V13, ../node/SPEC.md T7
T7|x|PORT [topic=be-local-functions] `utils/local_job_store.py` + local resolution paths in osparc/sampling/dakota so DEPLOYMENT_MODE=LOCAL serves uploaded/synthetic functions offline; uid-prefix routing; tests — implemented 2026-07-06 on `jgo/fullstack-csv` (commit `8f46023`) rather than its originally-designated `jgo/port-backend` branch, which is now dropped (root SPEC.md T18/T26); FE local-function browsing (existing-vs-new target selection) remains separate, still `.` on ../node/SPEC.md T7|I, V15
T8|x|PORT [topic=be-preserve-case] `json_serializer`/`helpers` `preserve_nested_keys` half (request+response+ingestion, both directions); remaining `utils/case_preserving.py`+`DataPreprocessor` orig-case round-trip carved out to §T16; tests `test_utils_helpers.py::TestPreserveNestedKeysForVariableNames` + end-to-end `test_flask_dakota_workflows.py::test_moga_preserves_irregular_case_variable_name_end_to_end`|I, V13, V29, B11
T9|.|PORT [topic=fullstack-logscale] accept per-variable log-scale flag in dakota request models → preprocess sample/train in log space, inverse on response; tests|V16, ../node/SPEC.md V12
T10|.|fix B1 (#467): anchor `LOCAL_STORE_DIR` to env/`__file__`, defer `mkdir(parents=True)` to first write; test cwd-independence|V17,B1
T11|.|fix B2 (#467): drop manual `jobIds` (or the snake key), let global serializer convert `job_ids` once; test ⊥ double-key collision|V18,B2
T12|x|fix B3 (#467): originally gated `list_local_*` merges + per-id local branches on `DEPLOYMENT_MODE=LOCAL` — REVERSED by T22/B21: local-store merging is now universal (∀ `DEPLOYMENT_MODE`), since local-store availability and remote-backend availability are orthogonal concerns; see V15,V34|V15,B3
T13|.|fix B4 (#467): `_parse_number` raise ValueError(row,col) on unparseable non-blank, blank→NaN; test rejects `"abc"`/swapped cols|V19,B4
T14|.|fix B5 (#467): narrow `_load_store` except to `(OSError, json.JSONDecodeError)`, backup before reset; test corrupt-json ⊥ wipe store|V20,B5
T15|x|PORT: inline vendored `mmux_python` → `src/mmux_flaskapi/dakota/` (6 used modules kept verbatim filenames: `lhs`,`dakota_object`,`funs_create_dakota_conf`,`funs_data_processing`,`funs_evaluate`,`wiofiles`; dropped 3 unused: `dakota_object_map`,`funs_git`,`funs_plotting`); rewrote internal cross-imports + blueprint imports (`dakota.py`,`sampling.py`) to `mmux_flaskapi.dakota.*`; removed `mmux-python` dep + `[tool.uv.workspace]`/`[tool.uv.sources]` + 6 dead transitive deps (gitpython,httpx,ipykernel,matplotlib,seaborn,tqdm) + coverage omit line from `pyproject.toml`; `uv sync` verified; full pytest suite green (439 passed) before+after|../SPEC.md T21,T2
T16|.|[topic=dakota-cleanup] dakota/ code-quality pass: fix known lhsmu/log_output/sanitize_varnames bugs (flagged in #477 review) w/ regression tests; raise dakota/ subpackage test coverage; deliberately do NOT invest in `funs_create_dakota_conf.py` — input-file-generation logic likely superseded by Dakota's new JSON input format (T17)|—
T17|.|RESEARCH: Dakota 6.24.0 introduced experimental JSON-format input files (`-json` CLI arg, Pydantic schema `python/dakota/spec/`; legacy NIDR parser deprecated but still available via `-parser legacy`) as the likely eventual replacement for `funs_create_dakota_conf.py`'s string-templated NIDR generation; evaluate migration once the JSON schema stabilizes (⊥ NIDR removed yet) — deferred, pairs T16|T16
T18|x|SuMo validation statistical rigor: (a) paired t-test on CV actual-vs-predicted residuals → surface bias significance (statistic+p-value) alongside MAE/RMSE in `/get_sumo_cv_accuracy_metrics`; (b) convergence analysis: rerun CV metrics at increasing training-sample-count subsets, expose `{n_samples,metric}` series for accuracy-vs-N plotting; tests|V26,V27,../node/SPEC.md T33
T19|x|correlation/sensitivity indices (#470): new endpoint `/dakota/compute_correlation_indices` computing per-input↔output Pearson+Spearman correlation from a UQ-style Monte Carlo sample set; single-plot multi-param sensitivity view (beyond current 3-param 1D/2D/3D limit); tests|V28,../node/SPEC.md T21
T20|x|Sobol' sensitivity indices migration (#470, #485): replace current Dakota `variance_based_decomp`/`parse_sobol_indices_output` path in `evaluate_sobol_indices` w/ scipy-based computation — Saltelli A/B/AB sampling locally honouring distributions, single `evaluate_sumo()` batch, `scipy.stats.sobol_indices` for first/total order; tests grounded vs Ishigami analytical reference (R1)|V32,../node/SPEC.md T22
T21|.|PORT [topic=be-preserve-case] remaining half of former T8: `utils/case_preserving.py` (`PreserveCaseTransform`/`FunctionVariablesDict`) + `DataPreprocessor` orig-case round-trip driven by function metadata (`has_preserve_case_metadata`) — NOT the Pydantic-wrapper design from closed/superseded PR #469 (its B8/B9, ../node/SPEC.md); design TBD|V14
T22|.|IMPORTANT (collaborator feedback #459/#464, PH Milestone 5 Task 5.5): investigate whether `{output}_std_hat` (V5) captures the surrogate's own GP/Kriging predictive variance at all, or only the spread of surrogate-mean predictions across MC/LHS-sampled inputs (input-driven only); #464's symptom (uncertainty band looks constant along x) is the signature of the latter — a real Kriging posterior variance should widen in sparsely-sampled regions; fix: combine both variance sources (law of total variance) into the reported `_std_hat` so UQ output reflects true combined uncertainty, not just input-parameter spread|V5
T23|x|second-order (pairwise) Sobol' indices for `/compute_sobol_indices` (#470 follow-up, pairs T20): closed-form Jansen/Saltelli estimator reusing T20's f_A/f_B/f_AB arrays (no extra surrogate evaluations); always returned (⊥ opt-in), response `sobolSecondOrder:{varA:{varB:float}}` symmetric ∀ pairs; validate vs Ishigami refs (R1)|V32,V35,../node/SPEC.md T22
T24|x|GRILLED (2026-07-13): UQ Stats modal backend support (pairs ../node/SPEC.md T34, ../SPEC.md T32): extend `UQWithUncertaintyResponse` (`/manual_uq_propagation_with_uncertainty`) w/ `p1,p5,p95,p99` percentile fields (reuse already-computed `hist_min`/`hist_max` for p1/p99; 2 new `np.percentile` calls for p5/p95) alongside existing `mean,std,q1,median,q3`; epistemic/aleatoric uncertainty decomposition ⊥ in scope here — real computation exists on branch `jgo/uq-uncertainty-propagation`, port tracked separately (relates to T22's combined-variance investigation); tests|I,T22
T25|x|Sobol' base sample count decoupled from shared UQ `numSamples` (fixed `SOBOL_BASE_SAMPLES=1024`, Saltelli scheme `N*(d_varying+2)`) + bootstrap confidence intervals added to first/total-order indices (95% CI, free — resamples existing f_A/f_B/f_AB evaluations); second-order heatmap excluded from CI display; tests incl. `SOBOL_BASE_SAMPLES==1024` assertion + CI-field presence/finiteness/ordering|V36,V37,../node/SPEC.md T37
T26|.|V41 fix (closes B23): `evaluate_sumo_manual_crossvalidation` guards the mapped output column `y1` (and `y1_variance`) presence in `predictions.dat`/`variances.dat` before access, skipping a fold whose surrogate Dakota failed to emit the output (append fold+file-scoped `warnings`, continue) instead of raising an opaque `KeyError('y1')` 500; ALWAYS returns 200 + NaN-filled arrays + `warnings` (⊥ hard 4xx). Regression tests: (a) forced missing-column case (monkeypatch `DakotaObject.run`/conf) asserting 200 + `warnings`; (b) real-data `material_sweep_first50_Osparc.csv` 50-job set, `output=delta_T_nerve`, posted through the REAL Dakota path asserting 200 + `warnings` — closes the B3-style blind spot behind B23|V41,B23
T27|x|PR #487 review (wvangeit): strengthen `test_dakota_funs_data_processing.py` (V44) w/ a mixed-distribution-types regression test (normal+uniform vars sampled together in 1 seeded call) — existing cases only exercise 1 distribution type per call, future-proofs against a per-type-generator regression|V44
T28|x|fix B29: `flask_manual_uq_propagation_with_uncertainty` noise `r` scaled by `sqrt(2)` (was bare `erfinv(...)`, std 1/sqrt(2)~=0.707 not 1); regression test pins reported `std` to the injected `std_hat` (mocked `evaluate_sumo`) rather than `std_hat/sqrt(2)`. Backported from `jgo/uq-uncertainty-propagation` (simpler pre-T18-decomposition code path on this branch)|V5,B29
T29|x|fix B30: add `required_completed_jobs(input_vars, floor=5) = max(floor, len(input_vars)+1)` helper (`dakota_models.py`) + apply to `JobVariableSelection` (via `minimum_completed_jobs` param, wired through `dakota.py`'s `_jobs_to_df` + `data_preprocessor_integration.py`) and all 6 request models' own hardcoded `< 5` checks (`SumoCrossValidationRequest`,`ManualUQPropagationRequest`,`SumoAlongAxesRequest`,`SumoGridEvaluationRequest`,`MOGAOptimizationRequest`,`SumoCVAccuracyMetricsRequest`); regression test (11 vars/11 jobs→400, 12 jobs→200). Backported from `jgo/uq-uncertainty-propagation`|V2,V30,B30
T30|x|PR #496 Copilot review: fix remaining `get_osparc_api()` unconditional-crash sites for real function/job-collection uids (`list_function_job_collections_for_functionid`, `list_function_jobs_for_functionid`, `_function_schema_vars`) + drop broad `except Exception`/`assert` in `upload_job_collection_csv`; tests|V46,B31
T31|x|backprop PR #496 Copilot review (remaining 2 open comments): reject CSV data rows whose cell count ≠ header (422 w/ row context, `_parse_uploaded_job_collection_csv`); make `local_job_store._save_store` atomic (temp file + `os.replace`, cleaned up on failure); tests|V47,V48,B33
T32|x|[topic=dakota-cleanup] dakota/ code-quality pass (jgo/dakota-cleanup): fix B34-B36 w/ regression tests; add `test_dakota_{lhs,wiofiles,funs_evaluate,funs_data_processing,object}.py` (0%→98% lhs.py, 21%→91% wiofiles.py, 74%→90% funs_evaluate.py, 70%→87% funs_data_processing.py, 80%→100% dakota_object.py); deliberately did NOT invest in `funs_create_dakota_conf.py` (61%, unchanged) — input-file-generation logic likely superseded by Dakota's new JSON input format (§R); full suite green (507 passed, 0 regressions)|V49,V50,V51,B34,B35,B36
T33|x|add `ty` (astral-sh) as flaskapi type checker: dev dep in pyproject.toml, `[tool.ty]` config, baseline-fix existing src/mmux_flaskapi (25 files, mixed type-hint coverage, ~22 legacy `# type: ignore` comments), untyped 3rd-party deps needing overrides (dakota.environment, itis-dakota, osparc, scikit-learn), local pre-commit hook (new flaskapi/scripts/run-ty-hook.sh mirroring eslint-node pattern, entry in .pre-commit-config.yaml scoped `files: ^flaskapi/`), Makefile targets (root + flaskapi/), CI wiring (.github/workflows/ci.yml prek job needs `make install-flaskapi-deps` before `uvx prek run` to let ty resolve imports); blocking in both pre-commit and CI; strictness/rule-level deferred to implementation time|V52
T34|x|refactor osparc.py: (1) collapse the duplicated local-merge/graceful-degradation logic across functions/job-collections/jobs into a single `_ResourceKind` dataclass + 3 shared generics (`_list_merged`,`_get_by_id`,`_list_merged_for_parent`) behind one `_get_osparc_api_if_available()` gate, deleting `_is_local_deployment_mode()`; (2) reverse B3/T12's `DEPLOYMENT_MODE=LOCAL` gate — local-store merging + graceful degradation now apply unconditionally, so a live-in-prod oSPARC deployment can still serve local/CSV-imported functions and a down oSPARC never 500s regardless of mode; (3) fix `get_function_job_status`/`get_function_job_outputs` to route real vs. local job uids the same way `get_function_job` already did (previously always called `get_osparc_api()` unconditionally, ⊥ local-uid support, unlike the analogous B18 fix); added autouse `default_osparc_reachable` fixture (conftest.py) patching `UsersApi.get_my_profile` so existing SDK-call-only test fixtures keep working under the now-universal availability probe; tests (512 passed)|V15,V46,V53,B3,B31,B34
T35|x|fix B38: guard `compute_cv_accuracy_metrics` against the zero-valid-CV-pairs case (return NaN metrics ⊥ `np.max` on an empty array); catch `compute_paired_ttest`'s pre-existing `ValueError` at the `flask_get_sumo_cv_accuracy_metrics` route level and substitute a NaN t-test result rather than 500ing, leaving `compute_paired_ttest` itself unchanged (its own <2-sample raise is asserted directly by `test_funs_evaluate_cv_stats.py::test_too_few_samples_raises`). Surfaced by `upstream/develop`'s merged-in `test_insufficient_jobs_for_dimensionality_returns_clean_400`, which expects 12 completed jobs / 11 inputs to return 200 — with this branch's stricter manual 5-fold CV (V41/B23), each fold's training subset (9-10 rows) is smaller than `required_completed_jobs`'s full-dataset threshold (12), so every fold can legitimately fail Dakota's GP build and previously crashed the endpoint instead of degrading; tests (`test_funs_evaluate_cv_stats.py`, `test_flask_dakota_workflows.py::TestSumoCVAccuracyMetrics`, 41 passed)|V54,B38
T36|x|fix B39: preserve hard rejection when any completed job lacks a selected variable, but report missing variable names, missing-row counts, total completed rows, all-fields-required rule, and aggregate available keys; regression `test_job_variable_selection_reports_incomplete_rows_explicitly`|V55,B39

## §B
id|date|cause|fix
B1|2026-06-16|#467 `local_job_store` `LOCAL_STORE_DIR=Path.cwd().parent.parent.parent` at import → cwd-dependent unpredictable path (pytest cwd ≠ container cwd), `mkdir` no `parents`|V17
B2|2026-06-16|#467 `osparc.py` normalized collection emits both `jobIds`+`job_ids`; global camel serializer rewrites `job_ids`→`jobIds` → key collision, one silently overwrites (iteration-order dependent)|V18
B3|2026-06-16|#467 `osparc.py` local fn/collection merges + per-id branches run unconditionally ∀ `DEPLOYMENT_MODE` → OSPARC deploy leaks leftover `runs_local` state, violates V15|V15
B4|2026-06-16|#467 `sampling._parse_number` swallows unparseable cell → `0.0` → silent scientific-data corruption (job looks completed w/ zeros fed to Dakota)|V19
B5|2026-06-16|#467 `local_job_store._load_store` bare `except Exception`→empty store; next `_save_store` overwrites corrupt file → unrecoverable loss of saved functions/collections/jobs|V20
B6|2026-06-19|old `_get_all_items` `while retrieved < list_len` had no empty-page guard → paginated oSPARC listing could spin forever on `items=[]`|V21
B7|2026-06-19|recursive camel/snake key converters mutated caller dict while walking nested structures → hidden side effects on shared payloads|V22
B8|2026-06-19|`sampling.test_job` loop checked `"FAILURE" not in job` (dict keys) instead of `job["status"]` → failed jobs could keep polling|V23
B9|2026-06-19|`OsparcApi._anonymize` default `m=None` could fully expose short strings → logging leaked whole secret prefix|V24
B10|2026-06-19|Dakota endpoints called `os.chdir()` per request → process-global cwd mutation and request cross-talk risk|V25
B11|2026-07-02|`to_snake_case_request`/`recursive_dict_keys_camel_to_snake` had no preserve-subtree exception (unlike FE's V24/B18 read-path fix) → any irregular-case variable name (e.g. "TissueConduc", not just "sigma_blood"-style all-lowercase) sent in `distributions`/`sliderValues`/`outputVarSelection`/`projectInputs`/job `inputs`/`outputs` got silently mangled on arrival; same gap independently found in `_get_all_items` ingestion (`max_depth=1` still recurses one level into variable-name dicts) and the global `after_request` response hook — all three route through the same two shared functions, fixed once|V29
B12|2026-07-06|`ManualUQWithUncertaintyRequest.validate_uncertainty_requirements` (pydantic, runs before Dakota ever executes) rejected every real request w/ 400 "UQ with uncertainty requires '{output}_std_hat' in job outputs" — it checked raw `function_jobs[].outputs` (actual simulation results, e.g. CSV-uploaded/oSPARC job data) for a pre-existing `_std_hat` key that is never present there; `_std_hat` is a derived quantity `evaluate_sumo()` computes from Dakota's `variances.dat` after fitting the surrogate, not part of training data. The endpoint's real (correct) check already existed downstream against `results` post-`evaluate_sumo()` (dakota.py ~L382); the premature pydantic check was redundant, wrong, and untested (0% coverage on its raise branch per htmlcov) — masked in tests only because the fixture artificially injected `_std_hat` into job outputs to make the (wrong) check pass|V5
B13|2026-07-06|`create_manual_uq_samples` seed silently ignored (RNG created then discarded, scipy drew from global state) — fixed independently on branch `fix/uq-seed-not-forwarded` off `develop` (unrelated to this feature branch's scope); see that branch's own `flaskapi/SPEC.md` §B/§V entries for the fix detail|—
B14|2026-07-07|Rebase conflict merged fullstack-csv's optional connection helper into strict `get_osparc_api()` itself, so `/list_jobs` and `/list_function_job_collections` performed a real `/v0/me` DNS/network preflight before calling the patched endpoint SDK in tests; expected random downstream `OsparcApiException` statuses were masked as 422 "not connected" after slow retries|V31
B15|2026-07-08|`compute_correlation_indices`/`compute_sobol_indices` key their results by input-variable name, not API field name, but `_DEFAULT_PRESERVE_NESTED_KEYS` only exempted request-side keys (`distributions`,`sliderValues`,etc) — the response's outer `correlations`/`sobol` key converted fine, but `recursive_dict_keys_snake_to_camel` still recursed into and mangled per-variable inner keys (e.g. multi-word snake_case names like "sigma_blood" → "sigmaBlood"); frontend's `inputVars[i]` lookup into the response dict silently missed the mangled key and defaulted to 0, rendering correlation/Sobol' plots as all-zero bars despite a valid backend computation|V29
B16|2026-07-08|`_dak_exec_static` only read back captured `./stdout`/`./stderr` on the success path of `study.execute()`; any failure (e.g. a malformed NIDR input file) propagated only Dakota's own generic top-level exception ("Dakota aborted: Unknown error 254") with the real diagnostic text silently discarded, making the true root cause of a Sobol'/UQ abort unrecoverable from the API error response or logs|V33
B17|2026-07-09|Real production `/compute_sobol_indices` request with `seed:0` (a legitimate, explicitly-chosen reproducibility seed) aborted with opaque "Dakota aborted: Unknown error 254"; root cause: `add_sampling_method` writes `seed = {seed}` verbatim into the Dakota NIDR conf for `variance_based_decomp`, and Dakota's own NIDR parser rejects `seed = 0` ("seed must be > 0") during `dakenv.study(...)` *construction* — before `.execute()` even runs, so B16's fix (which only wrapped `.execute()`) never captured the real diagnostic text either; two fixes: (a) `SobolIndicesRequest.seed` now `ge=1`, rejected with a clear 400 pre-Dakota; (b) `_dak_exec_static` now captures/enriches construction-time aborts too, as defense-in-depth for other malformed-conf cases|V33,V34
B18|2026-07-13|spec drift discovered: T20/T23 + V32/V34/V35 + §R1 documented a scipy-based Sobol' migration (Saltelli sampling, second-order estimator, Ishigami validation test) as already implemented ("x"/present-tense), but `evaluate_sobol_indices`/`flask_compute_sobol_indices` still call Dakota's native `variance_based_decomp` (stdout regex parse, first/total order only, `seed` still `ge=1` NIDR constraint); no scipy code, no `sobolSecondOrder` field, no Ishigami test exist in the actual codebase — spec content written ahead of code, apparently dropped/never-landed across a merge (`c00b362`/`58cccb2`) into this branch without anyone noticing the code didn't follow|V32,V34,V35 marked PLANNED; T20,T23 reverted to `.`
B19|2026-07-04|`evaluate_sumo_crossvalidation`'s `log_output` was hardcoded `""` → `_parse_crossvalidation_outputlogs` never had real Dakota stdout to parse → `/get_sumo_cv_accuracy_metrics` always fell through to "No surrogate quality metrics found.", never returned real metrics in production; fixed by switching the endpoint to reuse the already-working `evaluate_sumo_manual_crossvalidation` compute path (actual/predicted arrays) instead of Dakota log parsing|V26,V27
B20|2026-07-15|User-reported production error on `/sumo_cross_validation` ("Error during calculation, please contact support"; API body `{"error":"33 columns passed, passed data had 14 columns"}`): `load_data()`'s `.dat`/`.txt` branch did `pd.DataFrame(lines[1:], columns=sanitize_varnames(lines[0]))` with no row-length validation, so any Dakota-generated file (e.g. a fold's `predictions.dat`) whose header token count disagreed with a data row's token count raised pandas' bare, contextless `ValueError` straight through `handle_workflow_error()` — no file path, no fold number, no line number, leaving the actual malformed-file location undiscoverable from logs or the API response. Root cause of *why* a given fold's `predictions.dat` gets a mismatched shape (Dakota surrogate/parsing edge case under specific real training data) could not be pinned down further without the full failing dataset, which was unavailable for exact reproduction (only a partial-jobs subset was on hand, and that subset reproduces cleanly through all 5 folds); this bug entry hardens the diagnostic boundary rather than the unconfirmed upstream cause|V38
B21|2026-07-15|Follow-up to B20: same user hit the (now-diagnostic) V38 error a 2nd time in production, giving file:line evidence — `predictions.dat` fold 0 header 14 cols `['_eval_id','interface','x1'..'x11','y1']` but data line 12 only 10 cols, matching a real job's x3-x11+y1 values w/ x1,x2 (and the 2 metadata cols) entirely absent from that one row. Tried to confirm the mechanism (line-wrapping in Dakota's tabular writer was the leading hypothesis) by (a) locating the exact run dir from the error message — already gone/inaccessible from the dev machine, no cleanup code exists so its absence means it's simply on a different deployment, and (b) inspecting ~30 other locally-available real `predictions.dat` files byte-for-byte — none reproduce the short-row shape, and all show Dakota's fixed-width writer preserving ≥1 space between adjacent fields even when a value's string form overflows its column width, so simple token-merging is not confirmed either. Root cause of the Dakota-side mechanism remains UNCONFIRMED; rather than guess a fix for unverified Dakota-internal behavior, hardened the diagnostic further so the *next* occurrence carries the raw file bytes needed to confirm/refute it, without requiring separate filesystem access to a run dir that may not be reachable|V39
B22|2026-07-15|CONFIRMED (supersedes B21's "root cause remains UNCONFIRMED"): 3rd real production reproduction, this time inspected byte-for-byte directly inside the LIVE `mmux_vite-mmux-vite-backend-1` docker container (`docker exec ... cat -A`, confirming a single genuine newline-terminated line, ⊥ a missing-newline artifact) — Dakota's own tabular writer, during `list_parameter_study` surrogate evaluation, can write a row's `interface + leading continuous-variables` prefix (columns `1..k-1`) TWICE before writing the remaining columns `k..H-1` once; column 0 (`_eval_id`) is never repeated on the 2nd write, so it's genuinely lost from that row. General reconstruction formula `k = row_len - H + 1` derived and verified against the real captured row (H=14, row_len=24 → k=11: `eval_id+interface+x1..x9` first write, `interface+x1..x9` repeated w/ x9 differing only by float noise `0.554521` vs `0.554525`, then `x10,x11,y1` written once); exact Dakota-internal C++ mechanism still unknown, only the observable output pattern is confirmed (reproducibly, 3 times now) — possibly related to a pre-existing dev comment in `funs_create_dakota_conf.py` about a similar Dakota point-count quirk. User's chosen resolution: "implement logic to try healing; otherwise return a warning & full details but continue the fold" — implemented as V40|V40
B23|2026-07-15|User-reported production error on `/sumo_cross_validation`: generic 500 `{"error":"'y1'"}` (a bare `KeyError('y1')`) on a real 11-input / `delta_T_nerve` physics request. Root cause: `evaluate_sumo_manual_crossvalidation` (`funs_evaluate.py:285`) does `predictions_df[output_response]` (output_response = the mapped output `y1`) with NO guard that Dakota's `predictions.dat` actually contains that column; under real training data Dakota can fail to produce the requested-output surrogate for a fold and emit a `predictions.dat` whose header OMITS `y1`, so the bare `KeyError` bubbles through `handle_workflow_error` as an opaque 500. The V40 `on_malformed_row="heal_or_drop"` hardening only recovers SHORT/MISMATCHED *rows* (B22), NOT an ABSENT *column*, so it does not cover this failure mode. **Why e2e never caught it:** (1) the only test exercising the real, non-monkeypatched Dakota compute path — `test_sumo_cross_validation_success` — feeds `create_function_job_list(50, inputs=["x1"], outputs=["y"])`, fully synthetic `np.random.rand()` data that is always well-conditioned so Dakota always writes `y1`; it has never been run against production-shaped physics data; (2) several other sumo-CV tests `monkeypatch.setattr(..., evaluate_sumo_manual_crossvalidation, fake_eval)` (e.g. `test_sumo_cross_validation_preserves_prediction_suffixes_for_original_output_name`), bypassing the real compute path entirely; (3) the Playwright e2e (`tests/e2e/mock_osparc/data.py`) is also synthetic read-only mock and never hits this path; (4) no test asserts graceful degradation when `predictions.dat` lacks the output column, so this gap has 0 coverage — a textbook B3 ("mock + backend agree, no real-job shape exercised") blind spot, just one layer deeper (synthetic fixture instead of a hand-faked mock key). A RESIDUAL failure mode of the same endpoint — Dakota aborting a fold and never writing `predictions.dat` (observed non-deterministically on `material_sweep_first50_Osparc.csv`, `output=delta_T_nerve`) — raised the same opaque 500 via `load_data` rather than the column guard; closed by V43 (skip+continue on abort).|V41,V43
B24|2026-07-16|`/sumo_cross_validation` returned a flat dict with variable names as top-level keys (`{"delta_T_nerve": [...], "delta_T_nerve_hat": [...]}`); the global `after_request` camelCase serializer converted ALL keys including these variable names (`delta_T_nerve` → `deltaTNerve`, `delta_T_nerve_hat` → `deltaTNerveHat`), but the frontend read `data[selectedQoI]` / `data[`${selectedQoI}Hat`]` expecting the original snake_case names — for multi-word QoIs (e.g. `delta_T_nerve` from the real `material_sweep` dataset) both lookups silently returned `undefined`, so no plot rendered and the e2e timed out on `.js-plotly-plot`. Simple single-token names (e.g. `y1`) masked the bug because `snake_to_camel("y1_hat")` == `"y1Hat"` which matched the frontend's `${selectedQoI}Hat` pattern; the `_hat` suffix and the variable-name portion both get camelCased as one token stream, so only multi-word names diverge. Fixed by nesting the response under `cv_results` (V42) — added to `_DEFAULT_PRESERVE_NESTED_KEYS` so the serializer preserves all variable-name keys inside the subtree|V42
B25|2026-07-06|`create_manual_uq_samples` (funs_data_processing.py) called `np.random.default_rng(seed=seed)` but discarded the returned `Generator` instead of assigning it; `scipy.stats.norm.rvs`/`uniform.rvs` then drew from scipy's un-seeded global random state — the documented `seed` request param (for reproducibility) silently had zero effect, two identical requests produced different UQ samples|V44
B26|2026-07-09|PR #495 Copilot review: `node/package-lock.json` carried unrelated `"peer": true` churn (~51 lines, different local npm version) alongside an unrelated flaskapi PR, no `package.json` dep change to justify it — noisy diff/merge-conflict risk|no §V (regenerated-artifact hygiene, not app behavior); reverted to match main
B27|2026-07-09|PR #495 Copilot review: `flask_list_functions` logged the LOCAL-mode unreachable-oSPARC fallback at WARNING on every request — an expected, recurring condition while a dev backend stays down, spamming logs|V45
B28|2026-07-09|PR #495 Copilot review: renamed test `test_is_connected_property_short_circuits_after_failure` kept `_property_` in its name though `is_connected` is a method, not a property — misleading re: the API shape|no §V (test-naming, mechanical); renamed to `test_is_connected_short_circuits_after_failure`
B29|2026-07-09|`flask_manual_uq_propagation_with_uncertainty` generated noise via bare `r = erfinv(uniform(-1,1))` w/o the `sqrt(2)` factor required by the inverse-Gaussian-CDF identity (`Phi(x) = (1+erf(x/sqrt(2)))/2`); understated reported uncertainty by ~29% (std 1/sqrt(2) not 1). Backported from `jgo/uq-uncertainty-propagation` (B16 there)|V5
B30|2026-07-09|all 6 Dakota GP-surrogate-building request models validated a flat "≥5 completed jobs" regardless of input dimensionality; Dakota's surfpack GP surrogate build aborts (MODEL_ERROR/exit-250) once training points ≤ input dimensionality (e.g. 11 vars + 11 jobs), so high-dimensional requests could pass validation yet crash Dakota with an opaque "Dakota aborted: Unknown error 250". Backported from `jgo/uq-uncertainty-propagation` (B17 there)|V2,V30
B31|2026-07-09|PR #496 Copilot review: `list_function_job_collections_for_functionid`/`list_function_jobs_for_functionid`/`_function_schema_vars` called `get_osparc_api()` unconditionally for real (non-local) uids — in `DEPLOYMENT_MODE=LOCAL` w/ an unreachable backend this 500'd instead of degrading gracefully like the already-fixed `list_functions`/`list_function_job_collections`; `upload_job_collection_csv` also wrapped `parse_request_model(...)` in a broad `except Exception` (bypassing the registered `RequestParsingError` handler) and used `assert` for request validation (optimized away under `python -O`, surfaces as 500 not a controlled 4xx)|V46
B32|2026-07-09|Manually resolving the `origin/develop` (PR #495) merge conflict in `flask_list_functions` by uniformly picking develop's simpler hunk (predates the local_job_store/CSV-upload feature) silently dropped the `list_local_functions()` merge + its import that only existed on this branch — conflict markers resolved cleanly, file parsed/linted fine, no `git diff` review flagged it; caught only by re-running the full test suite (`test_local_functions_merged_in_local_mode`/`test_local_mode_degrades_gracefully_when_osparc_unreachable`) immediately after the merge|V15
B33|2026-07-09|PR #496 Copilot review (remaining 2 open comments): `_parse_uploaded_job_collection_csv` used `dict(zip(header,cells))` which silently truncates/misaligns a CSV data row whose cell count doesn't match the header instead of rejecting it; `local_job_store._save_store` wrote directly to the store file, risking a partially-written/corrupt file on an interrupted process or concurrent writers|V47,V48
B34|2026-07-04|`lhs.py` `elif method.lower() in ("lhsmu"):` — missing tuple comma made `("lhsmu")` a plain string, so `in` did substring-containment (e.g. `"u" in "lhsmu"` also True) instead of an exact-value check; flagged in #477 review, unresolved until now|V49
B35|2026-07-04|`funs_evaluate.evaluate_sumo_crossvalidation` hardcoded `log_output = ""` (stale "how to parse from stdout now?" TODO from #477 review) → cross-validation metrics parser always ran against an empty string, silently returning no metrics|V50
B36|2026-07-04|`funs_data_processing.sanitize_varnames` regex char-class `[^0-9a-zA-Z_*-+/]` placed `-` between `*` and `+`, forming an unintended range (matching only `*`/`+`) instead of a literal hyphen → real hyphens (e.g. default `get_results` key `-AFpeak`) were silently rewritten to `_`|V51
B37|2026-07-10|B3/T12's `DEPLOYMENT_MODE=LOCAL` gate on local-store merging conflated two orthogonal concerns: "is the local_job_store visible" and "is the remote oSPARC backend reachable" — a live prod deployment with a fully working oSPARC connection still could not surface CSV-imported/local functions, and each of the 3 resource kinds (functions/job-collections/jobs) + get-by-id/list-all/list-for-parent shapes had the merge/degrade skeleton hand-duplicated in every endpoint, which is how the earlier local-uid gap in `get_function_job_status`/`get_function_job_outputs` (unlike `get_function_job`) went unnoticed|V15,V46,V53
B38|2026-08-05|Merging `upstream/develop` into `jgo/sobol-indices` brought in `test_insufficient_jobs_for_dimensionality_returns_clean_400` (develop's simpler, non-K-fold CV implementation): 11 inputs / 12 completed jobs expected to pass `required_completed_jobs`'s gate (V2/V30/B30, `max(5, len(input_vars)+1)`) and return 200. On this branch, `/get_sumo_cv_accuracy_metrics` instead runs a real 5-fold `evaluate_sumo_manual_crossvalidation` (V41/B23) — `required_completed_jobs` only guarantees the FULL 12-job dataset exceeds 11 dims, not that each fold's ~9-10-row training subset does, so every fold legitimately failed Dakota's GP build. B23's per-fold "warn & continue" correctly left `all_predictions`/`all_stds` all-NaN, but the downstream `compute_cv_accuracy_metrics` had no guard for zero valid pairs: `np.max` on an empty `abs_residuals` array raised `ValueError: zero-size array to reduction operation maximum which has no identity`, caught by the route's generic `except Exception` and surfaced as an opaque 500 — the exact class of crash B22/B23 were meant to eliminate, just one layer downstream of where those fixes stopped. Confirmed NOT a develop-side bug (develop's own, differently-implemented CV path passes 12/11 cleanly in an isolated worktree check) and NOT introduced by the merge's conflict resolution (`funs_evaluate.py`/`dakota.py` had no conflicts in this merge; the crash is a latent gap in this branch's own B22/B23 code, only exposed once develop's test started exercising it). Fixed by V54|V54,T35
B39|2026-08-10|`JobVariableSelection` correctly hard-failed mixed-schema completed jobs, but its diagnostic said selected variables were "not found" while listing those same names in unioned available keys; it omitted which rows/fields violated the all-jobs requirement, leaving the user with a generic contradictory 400|V55
